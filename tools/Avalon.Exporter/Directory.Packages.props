<Project>

  <!-- Same arrangement as the emitters project beside it: versions come from the server's
       central file, so a divergence is not expressible. -->
  <Import Project="..\..\src\Directory.Packages.props"/>

  <PropertyGroup>
    <CentralPackageVersionOverrideEnabled>false</CentralPackageVersionOverrideEnabled>
  </PropertyGroup>

</Project>
