apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "avalon-api.fullname" . }}-test-connection"
  labels:
    {{- include "avalon-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "avalon-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
